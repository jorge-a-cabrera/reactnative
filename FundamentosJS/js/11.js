// Iteradores de Arreglos
const tecnologías = ['HTML', 'CSS', 'JavaScript', 'React', 'Node.js']

const nuevoArray = tecnologías.forEach(function(t){
    return t
})

const nuevaMap = tecnologías.map(function(t){
    return t
})

console.log(nuevoArray)
console.log(nuevaMap)
